//package monitor.examples.tests.protocols.buyTicket

object util {
  def approve(code : String): Boolean ={
    true
  }
  def invoice(code : String): Boolean ={
    true
  }
  def pay(payment : Int): Boolean ={
    true
  }
  def refuse(message : String): Boolean ={
    true
  }
}