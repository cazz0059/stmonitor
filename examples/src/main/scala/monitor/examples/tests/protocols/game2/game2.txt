

S_game=rec X.(
    +{
        !Guess(num: Int)[(num > 0) && (num < 10)].&{
            ?Correct(ans: Int)[ans==num],
            ?Incorrect().X
        },
        !Quit()
    }
)

testing for lemmas

always returns incorrect





different order lemma version -

S_game2 = rec X.(+{
        !Ask().&{
            ?Yes(answer : Boolean)[answer].+{
                !Guess1(num: Int)[(num > 0) && (num < 10)].&{
                    ?Correct1(ans: Int)[(ans==num) && (ans >= 10)],
                    ?Incorrect1().X
                },
                !Quit()
            },
            ?No(answer : Boolean)[!answer].+{
                !Guess2(num: Int, ans: Int)[(ans==num) && (ans >= 10)].&{
                    ?Correct2()[(num > 0) && (num < 10)],
                    ?Incorrect2().X
                },
                !Quit()
            }
        }
    }
)